<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.hireNoti">
	<insert id="insertHireNoti" parameterType="HireNotiVO">
		<selectKey resultType="int" keyProperty="hnNo" order="BEFORE">
			select hire_note_seq.nextval from dual
		</selectKey>
		insert into hire_noti 
		values(#{hnNo}, #{hnNotiTitle}, #{cmNo}, #{hnSalStart}, #{hnSalEnd},#{hnViews},#{hnStart},
		#{hnDeadline},#{hnHows},#{hnTask},#{hnEligiblity},#{hnPreference},#{hnStep},
		#{hnBenefits},#{hnDocument},#{hnAddr},#{hnDetailAddr},#{hnLat},#{hnLng},
		#{hnStatus},#{hnGender},#{hnRecruitNum},#{eduNo},#{careerNo})
	</insert>
	
	<select id="selectHiriNoti" parameterType="SearchVO" resultType="HireNotiVO">
		select * from hire_noti order by hn_no desc
	
	</select>
	
</mapper>








